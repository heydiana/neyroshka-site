name: Fix motion video switching once
on:
  push:
    branches: [main]
    paths: ['.github/workflows/fix-motion-switch-once.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fix video switching only
        shell: python
        run: |
          from pathlib import Path
          p=Path('works.html')
          s=p.read_text(encoding='utf-8')
          old="